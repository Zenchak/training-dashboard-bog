[build]
  command = "node build.js"
  publish = "dist"

# Corporate-network friendly front door.
# Netlify serves only as an HTTPS reverse proxy; the real Hub remains on the NAS
# behind Tailscale Funnel.
[[redirects]]
  from = "/*"
  to = "https://ds920plus.taild7745a.ts.net/:splat"
  status = 200
  force = true
